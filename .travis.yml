language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t mohitk/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true mohitk/docker-react npm run test -- --coverage